using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class LightningReaction : MonoBehaviour
{
    // Start is called before the first frame update
    abstract public void React();
}
